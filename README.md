# Demo CodeBuild Project (DEV)

This is a simple Python application used to test AWS CodeBuild
with an inline buildspec.

## What it does
- Prints a message when executed
- Used for DEV CI pipeline demo

## Trigger
- Manual CodeBuild run (Stage 1)
- CodePipeline trigger (Stage 2 – later)
